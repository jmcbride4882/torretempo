version: "3.9"
services:
  web:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - api
    restart: unless-stopped

  api:
    build: ./apps/api
    env_file: .env
    volumes:
      - ./data:/data
    restart: unless-stopped
